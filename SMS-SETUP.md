# 📱 Настройка SMS-верификации

## 🚀 Быстрая настройка

### 1. SMS.RU (Рекомендуется для России)

1. **Регистрация:**
   - Перейдите на https://sms.ru/
   - Зарегистрируйтесь и подтвердите аккаунт
   - Пополните баланс (от 100₽)

2. **Получение API-ключа:**
   - Войдите в личный кабинет
   - Перейдите в "Настройки" → "API"
   - Скопируйте API ID

3. **Настройка в коде:**
   ```javascript
   // В файле sms-config.js замените:
   api_id: 'YOUR_SMS_RU_API_KEY'
   // на ваш реальный API ID
   ```

### 2. SMSC.RU (Альтернатива)

1. **Регистрация:**
   - Перейдите на https://smsc.ru/
   - Зарегистрируйтесь
   - Пополните баланс

2. **Настройка:**
   ```javascript
   // В файле sms-config.js замените:
   login: 'YOUR_SMSC_LOGIN',
   password: 'YOUR_SMSC_PASSWORD'
   ```

### 3. Twilio (Международный)

1. **Регистрация:**
   - Перейдите на https://www.twilio.com/
   - Зарегистрируйтесь и получите пробный аккаунт

2. **Настройка:**
   ```javascript
   // В файле sms-config.js замените:
   account_sid: 'YOUR_TWILIO_SID',
   auth_token: 'YOUR_TWILIO_TOKEN',
   from_number: '+1234567890'
   ```

## 💰 Стоимость SMS

| Сервис | Цена за SMS в России | Особенности |
|--------|---------------------|-------------|
| SMS.RU | 1.5-2₽ | Быстрая доставка, хорошая репутация |
| SMSC.RU | 1.2-1.8₽ | Дешевле, больше настроек |
| Twilio | ~3₽ | Международный, много функций |

## 🔧 Переключение сервиса

В файле `sms-config.js` в функции `sendSMS()` измените:

```javascript
// Для SMS.RU:
return await sendSMSRu(phone, message);

// Для SMSC.RU:
return await sendSMSSMSC(phone, message);
```

## 🧪 Тестирование

При разработке на localhost код будет показываться в alert'е.
В продакшене SMS будет отправляться реально.

## 🛡️ Безопасность

1. **Ограничение частоты:** Добавьте лимит на количество SMS в минуту
2. **Валидация номеров:** Проверяйте формат российских номеров
3. **Логирование:** Ведите лог отправленных SMS для анализа

## 📋 Пример использования

```javascript
// Отправка кода
const code = '1234';
await sendSMS('+79991234567', `Код для входа: ${code}`);
```

## ❗ Важные моменты

- SMS доставляются не мгновенно (1-30 секунд)
- Некоторые операторы блокируют SMS с кодами
- Обязательно тестируйте на разных номерах
- Следите за балансом SMS-сервиса